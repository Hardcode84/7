kernel [[amdgpu_wave_size(64)]]
void wide(int32_t *out) {
  token output_done = token();
  simd<int32_t, 64> t = workitem_id<0>();
  simd<int32_t, 64> lane = lane_id<64>();
  output_done = join(output_done, store(t, out + lane));
  observe(output_done);
}
